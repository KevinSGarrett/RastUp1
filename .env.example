# RastUp1 — Example environment file
# Copy to .env (or export these in your shell / CI secrets) and fill in real values.

# === API keys===
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
ANTHROPIC_API_KEY=YOUR_ANTHROPIC_API_KEY_HERE
CURSOR_API_KEY=YOUR_CURSOR_API_KEY_HERE

# === Orchestrator core ===

# Root of this repo on your machine
RASTUP_REPO_ROOT=/mnt/c/RastUp1

# Model “tiers” used by the orchestrator helpers
ORCHESTRATOR_MODEL_LOW=gpt-4.1-mini
ORCHESTRATOR_MODEL_MEDIUM=gpt-4.1
ORCHESTRATOR_MODEL_HIGH=gpt-5

# Fallback pools used by review/plan helpers
ORCHESTRATOR_OPENAI_FALLBACK_MODELS=gpt-5,gpt-4.1,gpt-4o,gpt-4o-mini
ORCHESTRATOR_ANTHROPIC_FALLBACK_MODELS=claude-3-5-sonnet-20241022,claude-3-5-haiku-20241022

# Assistant‑manager wiring (Anthropic as “manager”, OpenAI as primary)
ORCHESTRATOR_AM_PROVIDER=anthropic
ORCHESTRATOR_AM_MODEL=claude-3-5-sonnet-20241022
ORCHESTRATOR_PD_PROVIDER=openai
ORCHESTRATOR_PD_MODEL=gpt-5

# Autopilot loop defaults (override as needed)
ORCHESTRATOR_AUTOPILOT_MAX_LOOPS=100
ORCHESTRATOR_AUTOPILOT_SLEEP_SECONDS=10

# Optional advanced routing (only if you enable these in code)
# ORCH_PRIMARY_LLM=openai:gpt-5
# ORCH_MANAGER_LLM=anthropic:claude-3-5-sonnet-20241022
# ORCH_CODER_LLM=openai:gpt-5
# ORCH_SUMMARY_LLM=openai:gpt-4.1
# ORCH_MAX_TOKENS_PER_RUN=250000
# ORCH_MAX_COST_PER_DAY=50.0

# Manager behavior toggles (optional)
# ORCH_MANAGER_ENABLED=1
# ORCH_MANAGER_REVIEW_EACH_ITER=1
# ORCH_MANAGER_AUTO_REQUEUE=1

# CI / helper flags (optional)
# ASSISTANT_MANAGER=anthropic

# Web app / Next.js env (add more as you start using real APIs)
# NEXT_PUBLIC_APP_ENV=local
