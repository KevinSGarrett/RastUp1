terraform {
  required_version = ">= 1.5.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = var.aws_region
}

# SAFE-MODE: plan-only skeleton resources
# queues, state machines, secrets — stubbed modules for plan validation
